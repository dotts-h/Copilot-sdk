# Tech-debt register

> Tracked, prioritized shortcuts and gaps. Severity = impact if it bites. Effort = cost to fix.
> Interest = ongoing cost of leaving it. Rank by interest × likelihood.

| # | Item | Location | Sev | Effort | Interest | Links | Trigger to pay down |
|---|------|----------|-----|--------|----------|-------|---------------------|
| 1 | Claude folder/`SKILL.md` skill model — per-skill resources + allowed-tools, loading skills from on-disk folders. Deferred from backlog #9 (the focused slice shipped instead). Needs runtime support for skill resources the SDK may not expose. | internal/ctxforge | low | L | low | [ADR 0003](adr/0003-claude-cli-style-agents-built-in-chat-agent-and-per-agent-tool-allowlist.md) | when on-disk skill folders or per-skill resources are actually requested |
| 3 | Docs say "single in-memory session"; the Hub is cookie-keyed multi-session. | docs/ARCHITECTURE.md, README | low | S | med | — | next docs pass |
| 5 | Desktop ships raw binaries only — no installers (.dmg/.msi/.deb/AppImage). "Binaries-first" was the chosen scope; packaging via the `wails3 package` CLI (Taskfile, icons, plist/NSIS) is deferred. | cmd/my-orchestra-desktop, .github/workflows/desktop.yml | low | M | low | [ADR 0006](adr/0006-desktop-shell-via-wails-v3-localhost-window.md) | when a non-technical install flow is needed |
| 6 | Wails v3 pinned to an **alpha** (`v3.0.0-alpha.98`); also forced the module to Go 1.25. API may shift before stable. | go.mod | med | S | med | [ADR 0006](adr/0006-desktop-shell-via-wails-v3-localhost-window.md) | when Wails v3 reaches a stable release |
| 7 | A Settings save refreshes the budget knobs (allowance, warn fraction, hard cap) only on the **editing** session; other concurrent cookie-keyed sessions pick the change up on their next session. Fine for the single-user localhost tool. | internal/web (`refreshBudget`) | low | S | low | [ADR 0008](adr/0008-budget-guardrails-soft-warn-and-hard-cap-gate.md) | when multi-session live-config propagation matters |
| 8 | Spend persistence writes the **whole ledger synchronously** in the single Hub pump goroutine on each `EvUsage` (O(n) full-file rewrite under `s.mu`). Sub-ms at this tool's one-record-per-turn volume, but it serializes the pump on disk IO. | internal/web (`session.go` EvUsage), internal/telemetry (`SpendStore.Append`) | low | M | low | [ADR 0009](adr/0009-persisted-spend-history-append-only-ledger.md) | when turn volume or session count makes the per-turn rewrite visible — batch/async-flush or switch to JSONL append |
| 11 | No **first-party embedded MCP server**: curated stdio servers need an external runtime (`npx`/`uvx`) on `PATH`, so an offline host has no out-of-the-box MCP capability (only config is seeded, not capability). | internal/bootstrap (`curatedMCPServers`), internal/copilot (`sdkclient.go` stdio wiring) | low | L | low | [ADR 0010](adr/0010-mcp-server-management-page-curated-defaults-disabled-with-preflight.md) | when a zero-dependency baseline is needed — embed a Go MCP server (sidecar exec, or in-process if the SDK exposes non-stdio registration) |
| 13 | A **rebound keyboard shortcut applies only on the next full page load**. A Settings save swaps `#main` only, but the live `<body data-keymap>` and the help overlay are rendered by the index shell, so the JS dispatcher keeps the old keymap until a reload. Consistent with the rest of Settings ("applied on your next session"); fine for a single-user localhost tool. | internal/web (`settings.go`, `server.go` `handleIndex`) | low | S | low | [ADR 0014](adr/0014-keybinding-surface-config-backed-keymap-with-minimal-js-dispatch.md) | when a rebind must take effect without reload — re-render the overlay + re-emit `data-keymap` (e.g. an OOB swap or a tiny `dataset` update) on a settings save |

## Paid

| # | Item | Paid by | Notes |
|---|------|---------|-------|
| 2 | Statusline token/credit totals were meter-global, not per-session. | item 3.2 (`feat/per-session-telemetry`) | `Server.sessionMeter` (a per-conversation `telemetry.Meter` on the account-wide meter's price book) is recorded alongside the global meter on each `EvUsage`; `renderStatline` reads it. Budget gauge / hard-cap / Telemetry stay account-wide. See [ADR 0011](adr/0011-per-session-telemetry-meter-for-the-statusline.md); guarded by `internal/web` `TestStatlineScopesTotalsToThisSession` / `TestTelemetryPageStaysAccountWide` and `internal/telemetry` `TestMeterPriceBookExposed`. |
| 4 | Web agent activation didn't compile the agent's full system message (instructions + skill prompts) — only model/effort/tools were applied. | PR #22 (`fix/agent-system-message`) | Both activation sites + clear path + startup now route through `Forge.Compile`; guarded by `internal/web` `TestAgentSystemMessageCompiledOnSelect` / `TestAgentClearCompilesGlobalContext`. See [REGRESSIONS #13](REGRESSIONS.md). |
| 9 | The Telemetry budget / month-to-date rows read the live (this-process) `Meter`, so they reset on restart even though the trend view did not. | item A1 / issue [0014](issues/0014-ledger-derived-budget-rows.md) (`claude/next-features-research-8aBvS`) | New pure `telemetry.MonthToDate(records, now)` (UTC calendar month); `telemetryPartial`, `renderCostFooter`, `/cost`, and the hard-cap `overCap` baseline now read month-to-date from the persisted `SpendStore`, so "remaining this month" survives a restart. The per-session statusline (`sessionMeter`, ADR-0011) and the live token split stay on the in-process meter — one source per surface. The shared `recordUsage` helper records every turn into both meters AND the ledger. Additive (no schema change). See [ADR-0016](adr/0016-ledger-is-source-of-truth-for-account-wide-budget-accounting.md); guarded by `internal/telemetry` `TestMonthToDate` and `internal/web` `TestBudgetRowsSurviveFreshMeter` / `TestCapBaselineReadsLedger` / `TestCostFooterReadsLedger` / `TestRecordUsageHitsBothMetersAndLedger`. |
| 12 | Workflow lanes surfaced only message/usage per step, and the offline demo exercised only the **sequential** path (parallel lane attribution was `SessionID`-keyed and unit-tested, but the mock returned one session id so the demo/e2e couldn't drive concurrent lanes). | item B1 / issue [0015](issues/0015-real-parallel-workflow-lanes.md) (`claude/next-features-research-8aBvS`) | `MockClient.CreateSession` now returns **distinct** ids (`mock-session-N`) and `streamDemoLane` tags its events with the lane's id, so a seeded **parallel** demo workflow drives concurrent lanes offline (a new e2e spec covers it). `handleRunEvent` reduces a lane's `EvToolStart/Progress/End` + `EvPermission` onto per-lane state, and `renderLanes` surfaces each lane's own tool timeline + inline permission by reusing the chat `renderToolCard`/`renderPermForm`; `handlePerm` is lane-aware (drops from the lane, OOB `#lanes` refresh). Escaping (ADR-0001) and `SessionID`-keyed attribution (REGRESSIONS "a workflow run owns the turn") held. See [ADR-0017](adr/0017-per-lane-tool-and-permission-surface-for-parallel-workflow-lanes.md); guarded by `internal/copilot` `TestCreateSessionReturnsDistinctIDs` and `internal/web` `TestWorkflowRunReducerParallelRoutesBySessionID` / `TestWorkflowLaneSurfacesToolTimeline` / `TestWorkflowLaneInlinePermission` / `TestWorkflowLaneToolTextEscaped` / `TestStreamDemoLaneTagsSessionID` / `TestParallelDemoRunDrivesConcurrentLanes`. |
| 15 | A multi-line snippet body inserted into the composer flattened to one line — the composer was a single-line `<input>` whose value-sanitisation strips newlines, so `fillSnippet` lost line breaks on insert. | item C2 / issue [0018](issues/0018-textarea-composer.md) (`claude/next-features-research-8aBvS`) | The composer is now a `<textarea name="prompt">` (Enter sends via `composerKeydown`→`requestSubmit`; Shift-Enter inserts a newline; `autosize` grows it). Inserting a multi-line snippet keeps its line breaks, and multi-line prompts work generally. UI-only (no contract/route change); the REGRESSIONS #8 `event.target===this` after-request guard and the keydown dispatcher's `INPUT/TEXTAREA/SELECT` skip both held. No ADR (Enter-to-send is the item's spec; autosize-with-cap is conventional). Guarded by `internal/web` `TestComposerRendersTextarea` / `TestComposerKeydownAndAutosizeWired` / `TestCommandsMenuPreservesMultilineSnippetBody`; e2e `ux.spec.ts` (Shift-Enter newline, bound-char-stays-text) + `e2e.spec.ts` (multi-line snippet insert keeps newlines). See [REGRESSIONS](REGRESSIONS.md) "the composer is a `<textarea>`". |
| 10 | No secrets/`Env` editor for MCP servers: the curated defaults were key-free and the MCP form didn't edit `Env` (only settable by hand in `forge.json`), blocking key-requiring servers (GitHub, web search) from the UI. | item C1 / issue [0019](issues/0019-mcp-secrets-env-editor.md) (`claude/mcp-secrets-env-editor`) | The MCP form now edits `Env` via masked key/value rows: a **secret** row persists only a `${VAR}` **reference** (the value field names the env var, never the secret), resolved at session start in `web.MCPServerSpecs` behind the `s.lookupEnv` seam (default `os.Getenv`) — **no secret at rest**, following the `config.GitHubTokenEnv` precedent. An unresolved reference is left unset (never sent as the literal) and flagged in the page preflight. CONTRACTS §4 flipped; the 0600 `secrets.json` store stays the documented escape hatch for a no-shell install. See [ADR-0020](adr/0020-mcp-secrets-via-env-var-reference-indirection.md); guarded by `internal/web` `TestMCPServerSpecsResolvesEnv` / `TestMCPUpdateRoundTripsEnvViaEditor` / `TestMCPCreateRejectsMalformedSecretRef` / `TestMCPPreflightFlagsUnresolvedEnv` / `TestMCPEditFormShowsEnvRows`; e2e `e2e.spec.ts` "Env editor stores a secret as a masked ${VAR} reference"; [REGRESSIONS #16](REGRESSIONS.md). |
